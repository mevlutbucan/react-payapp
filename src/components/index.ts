import AppProvider from './AppProvider';
import Card from './Card';
import Form from './Form';

export { AppProvider, Card, Form };

const PayApp = {
  Provider: AppProvider,
  Card: Card,
  Form: Form,
};
export default PayApp;
